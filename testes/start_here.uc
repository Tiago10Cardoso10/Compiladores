// there are no expressions in this case (no AST annotations)

// global symbols first

int    a, b;
double c;
char   d, e;
short  f;

// then functions

short g(int h, double i, char j, short k) {
    int l;
}
